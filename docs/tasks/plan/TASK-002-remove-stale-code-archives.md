# TASK-002: remove-stale-code-archives

Lifecycle: tracked in `docs/tasks/active_spec.md` and `docs/tasks/progress.md`
Owner Flow: plan-agent -> execute-agent -> audit-agent

## Background

TASK-001 is blocked: `tsconfig.json`'s `"include": ["**/*.ts", "**/*.tsx"]` captures old code in `docs/_archive/` (both `raw-ai-scaffold-20260606-1724/` and `pre-tooling-scaffold-20260606/`), producing ~70 type errors that block `tsc --noEmit` and `npm run build`.

**User decision:** The archived old code has no reference value. Delete both archive directories rather than modify tsconfig.json. The `docs/00_design/` files are the design source of truth; the old scaffold is noise.

## Goal

Delete the two stale archive directories to clear all tsc/build errors originating from archived code. This unblocks TASK-001.

## Non-goals

- Modifying `tsconfig.json` (exclude list)
- Deleting `docs/_archive/` directory itself (unless completely empty after deletions)
- Modifying any app/, docs/00_design/, AGENTS.md, package.json, or other forbidden files

## Design Source

TASK-001 audit report (P0 finding: tsconfig captures archives).

## Files In Scope

| File/Dir | Action |
|----------|--------|
| `docs/_archive/raw-ai-scaffold-20260606-1724/` | Delete recursively |
| `docs/_archive/pre-tooling-scaffold-20260606/` | Delete recursively |
| `tsconfig.tsbuildinfo` | Delete if exists |
| `docs/tasks/plan/TASK-002-remove-stale-code-archives.md` | Create (this file) |
| `docs/tasks/execution-log/TASK-002-remove-stale-code-archives.md` | Create |
| `docs/tasks/audit-report/TASK-002-remove-stale-code-archives.md` | Create |
| `docs/tasks/progress.md` | Add TASK-002 row; update TASK-001 Next Action |
| `docs/tasks/active_spec.md` | Point to TASK-002 |

## Forbidden Changes

- docs/00_design/
- docs/reference/
- docs/tasks/README.md
- docs/tasks/*TEMPLATE* files
- AGENTS.md
- app/
- package.json
- package-lock.json
- tsconfig.json
- README.md
- .gitignore
- node_modules/

## Acceptance Criteria

- [AC1] `docs/_archive/raw-ai-scaffold-20260606-1724/` does not exist.
- [AC2] `docs/_archive/pre-tooling-scaffold-20260606/` does not exist.
- [AC3] `tsconfig.tsbuildinfo` does not exist (or regenerated by incremental build — if so, .gitignore covers it).
- [AC4] `node .\node_modules\typescript\bin\tsc --noEmit` reports zero errors from `docs/_archive/`.
- [AC5] `npm.cmd run build` passes (SWC warning acceptable).
- [AC6] `package-lock.json` is preserved.
- [AC7] `app/` skeleton files are unchanged.
- [AC8] `docs/_archive/` directory may remain if empty — document in log.

## Test First Plan

1. Confirm both archive directories exist (`Test-Path`).
2. Delete both directories.
3. Delete `tsconfig.tsbuildinfo`.
4. Run `tsc --noEmit` — must pass (zero errors, or only pre-existing non-archive errors).
5. Run `npm run build` — must succeed.
6. Verify `package-lock.json` and `app/` untouched.

## Implementation Strategy

1. Verify target directories exist.
2. Delete `docs/_archive/raw-ai-scaffold-20260606-1724/` recursively.
3. Delete `docs/_archive/pre-tooling-scaffold-20260606/` recursively.
4. Delete `tsconfig.tsbuildinfo`.
5. Run tsc and build verification.
6. Update progress.md: TASK-002 done, TASK-001 ready for re-audit.

## Risks

- **None.** The archives contain only copies of the old scaffold which has already been replaced by TASK-001's new skeleton. No active code references these directories.

## Rollback

Not applicable — these are stale archive copies with no production value. If needed, the old scaffold still exists in git history.

## Approval

Self-approved per user explicit instruction to create and execute this fix task.
